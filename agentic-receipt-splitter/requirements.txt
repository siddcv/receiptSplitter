# Core web framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22
# Pin to the currently installed version to avoid Windows uninstall script issues
python-dotenv==1.0.1

# Data and validation
pydantic==2.12.5

# LangGraph and checkpointer
langgraph==1.0.7
langgraph-checkpoint-postgres==3.0.4

# Postgres driver
psycopg[binary,pool]==3.3.2

# LLM provider (used later in vision node)
langchain-google-genai==4.2.0
